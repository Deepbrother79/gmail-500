name: TEST Scraper (No Supabase)

on:
  # Solo esecuzione manuale per test
  workflow_dispatch:

jobs:
  test-scraping:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: 🧪 Run TEST scraper (without Supabase)
        run: node test-scraper-only.js

      - name: Report status
        if: always()
        run: |
          if [ $? -eq 0 ]; then
            echo ""
            echo "╔════════════════════════════════════════╗"
            echo "║  ✅ TEST COMPLETATO CON SUCCESSO      ║"
            echo "╚════════════════════════════════════════╝"
            echo ""
            echo "✅ Il browser funziona"
            echo "✅ I dati vengono estratti correttamente"
            echo ""
            echo "💡 Prossimo passo: usa il workflow 'Gmail500 Scraper'"
            echo "   per salvare i dati su Supabase"
          else
            echo ""
            echo "╔════════════════════════════════════════╗"
            echo "║  ❌ TEST FALLITO                       ║"
            echo "╚════════════════════════════════════════╝"
            echo ""
            echo "Controlla i logs sopra per vedere l'errore"
            exit 1
          fi
